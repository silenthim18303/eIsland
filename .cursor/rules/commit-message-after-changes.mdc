---
description: Prepare an authorized commit operation after code or file changes
alwaysApply: true
---

# 修改结束后的待授权提交操作

完成任何会改动仓库文件的任务后，必须生成一个可直接执行的 git 提交操作，但不要主动执行，等待用户明确授权。

要求：

- 提交操作必须基于本次实际改动，不要包含未完成或未验证的内容。
- 优先使用项目已有提交风格，例如 `feat(scope): summary`、`fix(scope): summary`、`refactor(scope): summary`、`docs(scope): summary`。
- 提交操作必须包含明确的 `git add -- <paths>` 和 `git commit -m`，只暂存与本次任务相关的文件。
- 如果改动包含多类事项，提交信息正文用 1-3 个短句说明用户价值和关键范围。
- 未获得用户授权前，不要把提交命令当作已执行动作；用户授权后直接调用终端执行已准备好的提交操作。
- 最终回复只说明提交操作已准备好并等待授权，不要把命令作为普通文本打印出来。
- 如果本轮没有文件改动，明确说明“不需要提交操作”。