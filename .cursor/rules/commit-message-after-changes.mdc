---
description: Require a commit message draft after code or file changes
alwaysApply: true
---

# 修改结束后的提交信息

完成任何会改动仓库文件的任务后，必须在最终回复中给出一条可直接使用的 git 提交信息草稿。

要求：

- 提交信息必须基于本次实际改动，不要写未完成或未验证的内容。
- 优先使用项目已有提交风格，例如 `feat(scope): summary`、`fix(scope): summary`、`refactor(scope): summary`、`docs(scope): summary`。
- 如果改动包含多类事项，提交信息正文用 1-3 个短句说明用户价值和关键范围。
- 只有在用户明确要求提交时，才执行 `git commit`；默认只提供提交信息草稿。
- 如果本轮没有文件改动，明确说明“不需要提交信息”。